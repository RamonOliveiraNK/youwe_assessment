<?php

use Drupal\views\Views;

/**
 * @param $vars
 */
function youwe_module_preprocess_node(&$vars) {

  // Get this node's type.
  $node_type = &$vars["node"]->getType();

  if ($node_type == 'resume') {
  }

}
